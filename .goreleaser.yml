before:
  hooks:
    # you may remove this if you don't use vgo
    - go mod tidy
builds:
-
  env:
    - CGO_ENABLED=1
  goos:
    - linux
  goarch:
    - amd64
archives:
-
  format: gz
  files:
  - none*
checksum:
  name_template: 'checksums.txt'
snapshot:
  name_template: "{{ .Tag }}-next"
changelog:
  sort: asc
  filters:
    exclude:
    - '^docs:'
    - '^test:'
